import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
      position: "absolute",
      top: 50,
      left: 20,
      right: 20,
      padding: 10,
      borderRadius: 5,
      alignItems: "center",
      zIndex: 1,
    },
    text: {
      color: "#FFFFFF",
      fontSize: 14,
    },
  });